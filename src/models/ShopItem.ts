export type ShopItem = {
  name: string;
  value: number;
  id: string;

  // constructor(itemName: string, itemValue: number = 1, itemId?: string) {
  //   this.name = itemName;
  //   this.value = itemValue;

  // }
};
// this.id = itemId ? itemId : Math.random().toString();
